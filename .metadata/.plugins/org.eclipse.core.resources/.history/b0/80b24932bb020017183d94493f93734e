package ControlesDecisoesLoopsExercícios;

import java.util.Scanner;

import javax.xml.bind.annotation.adapters.XmlJavaTypeAdapter.DEFAULT;

public class ExreciciosWhileFor {

	public static void main(String[] args) {

		int num1 = 0;
		int num2 = 0;
		int num3 = 0;
		Scanner scan = new Scanner(System.in);

		// Exrcício 1 - Pedir até que o valor numérico seja entre 0 e 10.
		/*
		 * System.out.print("Dê uma nota entre 0 e 10: "); num1 =
		 * scan.nextInt(); while (num1 < 0 || num1 > 10) { System.out.print(
		 * "Dê uma nota entre 0 e 10 válida: "); num1 = scan.nextInt(); }
		 * 
		 * System.out.println("O número " + num1 + " é válido");
		 */

		// Exercício 2 - Ler usuário e senha. Senha não pode ser igual ao
		// usuário

		String usuario;
		String senha;
		boolean diferente = false;

/*		do {
			System.out.print("Digite seu usuário: ");
			usuario = scan.next();
			System.out.print("Digite a senha: ");
			senha = scan.next();
			
			if (senha.equalsIgnoreCase(usuario)) {
				System.out.println("Usuário e senha iguais, digite novamente.");
			}else{
				diferente = true;
			}

		} while (!diferente);

		System.out.println("Usuário e senha válidos!");
*/
		
		//Exercício 3 - Ler e validar NOME, IDADE, SALÁRIO, SEXO e ESTADO CIVÍL
		
		String nome;
		int idade = 0;
		double salario = 0;
		String sexo;
		String estadoCivil;
		boolean valido = false;
		
		do{
			System.out.println("Seu nome: ");
			nome = scan.next();
				if(nome.length() > 3){
					valido = true;
				}else{
					System.out.println("Nome deve ter mais de 3 letras. Digite novamente.");
				}
		}while(!valido);
		
		do{
			System.out.print("Sua idade: ");
			idade = scan.nextInt();
				if(idade > 0 && idade < 150){
					valido = true;
				}else{
					valido = false;
					System.out.print("Idade deve ter entre 0 e 150. Digite novamente ");
				}
		}while(!valido);
		
		do{
			System.out.print("Seu salário: ");
			salario = scan.nextDouble();
				if(salario > 0){
					valido = true;
				}else{
					valido = false;
					System.out.print("Salário deve ser maior que 0. Digite novamente ");
				}
		}while(!valido);
		
		do{
			System.out.print("Seu sexo: ");
			sexo = scan.next();
				if(sexo.length() == 1){
					switch(sexo){
					case "M": 
					case "m":
					case "f":
					case "F": valido = true;break;
					
					default: valido = false;
					System.out.print("Sexo deve Feminino (F/f) ou Masculino (M/m). Digite novamente ");
					}
					
				}else{
					valido = false;
					System.out.print("Sexo deve Feminino (F/f) ou Masculino (M/m). Digite novamente ");
				}
		}while(!valido);
	}

}
